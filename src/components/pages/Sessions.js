import React from 'react';
import Navbar from "../Navbar";

const Sessions = () => {
    return (
        <div>
            <Navbar />
            <h1>Sessions</h1>
        </div>
    );
};

export default Sessions;