import React from 'react';
import Navbar from "../Navbar";

const Schedule = () => {
    return (
        <div>
            <Navbar />
            <h1>Schedule</h1>
        </div>
    );
};

export default Schedule;